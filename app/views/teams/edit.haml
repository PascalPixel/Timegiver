.container
  .row
    .col-lg-8.offset-lg-2.col-md-10.offset-md-1
      .card
        .card-header.clearfix
          .pull-sm-left
            %ol.breadcrumb
              %li.breadcrumb-item
                = link_to teams_path do
                  %i.mdi.mdi-account-multiple
                  = t('shared.navbar.teams')
              %li.breadcrumb-item= link_to @team.name, team_path(@team), style: "color:##{@team.color}"
              %li.breadcrumb-item.active Edit
          .pull-sm-right
            = link_to 'Cancel', team_path(@team), class: 'btn btn-secondary'

        = render 'form'
