.clearfix
  .pull-sm-left
    %ol.breadcrumb
      %li.breadcrumb-item
        = link_to users_path(team_id: @team) do
          %i.mdi.mdi-account
          = t('shared.navbar.users')
      %li.breadcrumb-item.active
        %i.mdi.mdi-account-plus
        Add
  .pull-sm-right
    = link_to 'Cancel', users_path(team_id: @team), class: 'btn btn-secondary'
%hr
= render partial: 'form'
